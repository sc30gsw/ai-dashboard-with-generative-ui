import { openuiChatLibrary } from "@openuidev/react-ui/genui-lib";

/** Chat generative UI uses openuiChatLibrary primitives only — compose per intent, no monolithic board. */
export const genuiLibrary = openuiChatLibrary;
